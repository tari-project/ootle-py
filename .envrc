# direnv configuration — committed. No personal data here.
#
# Loads project-local environment variables from `.env` if it exists.
# `.env` is gitignored; copy `.env.example` to `.env` and edit it to your
# machine's paths. After creating or modifying `.env`, run `direnv allow`
# once to authorise the load.

dotenv_if_exists
